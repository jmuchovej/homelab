---
originRequest:
  originServerName: "external.${TLD}"

ingress:
  - hostname: "${TLD}"
    service: &svc https://cilium-gateway-external.kube-system.svc.cluster.local:443
  - hostname: "*.${TLD}"
    service: *svc
  - service: http_status:404
