"""
Homelab CLI Tools.

A command-line interface for managing my homelab (The Rebellion) infrastructure,
primarily for things that `nix` can't (or shouldn't) do on system rebuilds.
"""
from homelab.commands import cli
