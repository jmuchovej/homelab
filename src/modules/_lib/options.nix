## Tiny option-shape helpers. `enabled` and `disabled` make `enable = true/false`
## reads slightly more naturally at call sites.
_: {
  _rbn-lib = {
    enabled = {
      enable = true;
    };
    disabled = {
      enable = false;
    };
  };
}
