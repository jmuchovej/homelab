---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: prowlarr
spec:
  dependsOn:
    - name: prowlarr-db
    - name: envoy-config
      namespace: network
  interval: 1h
  path: ./src/kubernetes/apps/media/prowlarr/app
  postBuild:
    substituteFrom:
      - kind: Secret
        name: cluster-settings
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: media
  wait: true
