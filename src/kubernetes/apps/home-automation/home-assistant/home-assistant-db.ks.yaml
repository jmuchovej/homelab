---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: hass-db
spec:
  dependsOn:
    - name: cloudnative-pg
      namespace: databases
    - name: onepassword-connect
      namespace: external-secrets
  interval: 1h
  path: ./src/kubernetes/components/cnpg-database
  postBuild:
    substitute:
      # `hass`, not `home-assistant`: renaming would orphan da's imported
      # volume, which carries the NixOS source database's name
      APP: hass
      DB_SIZE: 10Gi
      # convention: DB LBs live at .5x within the cluster's LB pool
      DB_LB_OCTET: "52"
  targetNamespace: home-automation
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  wait: true
