---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard
spec:
  secretName: wildcard-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
  commonName: ${DC_DOMAIN}
  dnsNames:
    - ${DC_DOMAIN}
    - "*.${DC_DOMAIN}"
