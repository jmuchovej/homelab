---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No namespace: transformer — every resource here is a cluster-scoped CCNP.
resources:
  - ./allow-egress.yaml
  - ./allow-ingress.yaml
