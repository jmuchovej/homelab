---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &name authentik
spec:
  chartRef:
    kind: OCIRepository
    name: *name
  interval: 1h
  # worker boot (migrations + blueprint import) regularly exceeds Helm's 5m default
  timeout: 15m
  valuesFrom:
    - kind: Secret
      name: &secrets authentik-secrets
      valuesKey: secret_key
      targetPath: authentik.secret_key
    - kind: Secret
      name: *secrets
      valuesKey: bootstrap_token
      targetPath: authentik.bootstrap_token
    - kind: Secret
      name: *secrets
      valuesKey: bootstrap_password_hash
      targetPath: authentik.bootstrap_password_hash
    - kind: Secret
      name: *secrets
      valuesKey: tofu_token
      targetPath: authentik.tofu_token
    - kind: Secret
      name: *secrets
      valuesKey: postgres_password
      targetPath: authentik.postgresql.password
    - kind: Secret
      name: *secrets
      valuesKey: email_username
      targetPath: authentik.email.username
    - kind: Secret
      name: *secrets
      valuesKey: email_password
      targetPath: authentik.email.password
  values:
    global:
      volumes:
        - name: media
          persistentVolumeClaim:
            claimName: authentik-media
      volumeMounts:
        - name: media
          mountPath: /media

    blueprints:
      configMaps:
        - authentik-blueprints

    authentik:
      postgresql:
        host: authentik-db-rw
        name: authentik
        user: authentik
        # persistent connections: one DNS lookup per thread per 5min instead
        # of one per request, which matters because the cluster drops ~1% of
        # DNS packets. conn_health_checks MUST accompany this — without it a
        # connection closed server-side (db restart, failover) surfaces as a
        # 500 on the next request instead of being transparently reopened.
        conn_max_age: 300
        conn_health_checks: true
      email:
        host: smtp.mailgun.org
        port: 587
        use_tls: true
        from: homelab@${DOMAIN}

    postgresql:
      enabled: false

    server:
      replicas: 1
    worker:
      replicas: 1
